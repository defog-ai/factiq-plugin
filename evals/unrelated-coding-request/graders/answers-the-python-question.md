---
type: llm
focus: last_message
weight: 3
---
The final message answers the Python question. It must say that `list.sort()` sorts the list in place and returns None, and that `sorted()` leaves the original unchanged and returns a new list.

Pass if both points are made, in any wording. Fail if the model asks for clarification instead of answering, or if it talks about economic data, charts, or FactIQ.
